name: Build OCI image for hello

on:
  push:
    branches: [main]
    paths-ignore: ['*.md', 'LICENSE', '.gitignore']
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    uses: appjail-makejails/workflows/.github/workflows/daemonless-build.yaml@main
    with:
      image_name: hello
    secrets: inherit
